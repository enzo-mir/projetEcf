import React from "react";
import { Outlet } from "react-router-dom";

const AdminConfig = () => {
  return (
    <>
      <div>AdminConfig</div>
      <Outlet />
    </>
  );
};

export default AdminConfig;
